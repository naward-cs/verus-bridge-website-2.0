// 'use client';

import React from 'react'

// import {useGetTokens} from '@/lib/hooks/tokens'

const RefundsAvailable = () => {
  // const {tokenList} = useGetTokens()

  return <div>RefundsAvailable</div>
}

export default RefundsAvailable
